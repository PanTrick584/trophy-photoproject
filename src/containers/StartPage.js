import { SectionWrapper } from "../styled/SectionWrapper"

const StartPage = () => {
    return <SectionWrapper>
                <h2>HOME</h2>
            </SectionWrapper>
}

export default StartPage